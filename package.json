{
  "name": "openlog",
  "version": "1.0.0",
  "description": "OpenLog - A logging and build-sharing platform",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --experimental-sqlite --import tsx --watch src/index.ts",
    "build": "tsc",
    "start": "node --experimental-sqlite dist/index.js",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "logging",
    "builds",
    "hono",
    "sqlite"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@hono/zod-validator": "^0.7.6",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "hono": "^4.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
